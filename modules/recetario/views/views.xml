<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="recetario.list">
      <field name="name">recetario list</field>
      <field name="model">recetario.recetario</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="recetario.action_window">
      <field name="name">recetario window</field>
      <field name="res_model">recetario.recetario</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="recetario.action_server">
      <field name="name">recetario server</field>
      <field name="model_id" ref="model_recetario_recetario"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="recetario" id="recetario.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="recetario.menu_1" parent="recetario.menu_root"/>
    <menuitem name="Menu 2" id="recetario.menu_2" parent="recetario.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="recetario.menu_1_list" parent="recetario.menu_1"
              action="recetario.action_window"/>
    <menuitem name="Server to list" id="recetario" parent="recetario.menu_2"
              action="recetario.action_server"/>
    -->
  </data>
</odoo>